on: push

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - run: pwd
        - run: ls
    job2:
        runs-on: windows-latest
        steps:
        - run: (Get-Location).Path
        - run: vcpkg.exe install --x-manifest-root="D:/a/learn_action/learn_action"